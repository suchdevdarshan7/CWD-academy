const { sum, difference, multiply } = require('./maths.js')

const { multiply } = require('./maths.js')

const add = sum(10, 20, 20);

const diff = difference(10, 20, 20);

const multi = multiply(10, 20, 30);


console.log(add)
console.log(diff)
console.log(multi)